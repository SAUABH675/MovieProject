# Movix Backend & DB Fix TODO

## Steps:
- [x] Step 1: Updated backend/package.json with dependencies
- [x] Step 2: npm --prefix backend install (deps installed)
- [ ] Step 3: Edit backend/.env with TMDB_TOKEN
- [ ] Step 4: cd backend && npm start (start backend server)
- [x] Step 5: Install sqlite3 CLI: npm install -g sqlite3
- [x] Step 6: Query DB (use npx sqlite3 backend/movix.db; tables created, data populates on API calls)
- [ ] Step 7: Test frontend (Vite reloads, APIs now work)
- [ ] Complete: Backend running, DB viewable via CLI

Current: Starting Step 1.

